<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ch.admin.mapper.AccountMapper">

    <select id="getAccount" resultType="com.ch.admin.bean.Account" parameterType="long">
        select * from tb_emp1 where id = #{id}
    </select>

</mapper>